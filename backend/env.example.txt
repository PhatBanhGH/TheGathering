# Copy this file to .env and fill in your values

# Server Configuration
PORT=5001
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/gather-town

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-very-secret-key-change-this-in-production

# Client URL (Frontend)
CLIENT_URL=http://localhost:5173
# Additional allowed frontend origins (comma-separated)
CLIENT_URLS=
# Netlify site name only (without .netlify.app), e.g. gather-demo
NETLIFY_SITE_NAME=
# Allow Netlify preview/branch deploy origins like:
# https://deploy-preview-123--<NETLIFY_SITE_NAME>.netlify.app
# https://feature-branch--<NETLIFY_SITE_NAME>.netlify.app
ALLOW_NETLIFY_PREVIEWS=false

# Email Configuration (for OTP) – Gmail: use App Password, not account password
# https://support.google.com/accounts/answer/185833
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google reCAPTCHA (Optional)
GOOGLE_RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key

